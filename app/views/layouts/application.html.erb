<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <link rel="icon" href="<%= image_path "logo-black.png" %>">

  <meta property="og:url"                content="<%= (yield(:url) || "https://elephant-and-castle.herokuapp.com/").squish %>" />
  <meta property="og:type"               content="article" />
  <meta property="og:title"              content="<%= (yield(:title) || "Elephant & Castle").squish %>" />
  <meta property="og:description"        content="<%= (yield(:introduction) || "Elephant & Castle").squish %>" />
  <meta property="og:image"              content="<%= (yield(:image) || image_path "banner.jpg").squish %>" />

  <title>Elephant &amp; Castle</title>
  <%= csrf_meta_tags %>
  <%= action_cable_meta_tag %>
  <%= stylesheet_link_tag    'application', media: 'all' %>
</head>
<body>
  <%= render 'shared/navbar', categories: @categories %>
  <%= yield %>
  <%= render 'shared/footer' %>

  <%= javascript_include_tag 'application' %>
  <%= cloudinary_js_config %> <!-- Append this after the main `javascript_include_tag`. -->

</body>
</html>
