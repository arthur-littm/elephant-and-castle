<div class="text-center">
  <%= form_for(@like) do |f| %>
  <%= f.hidden_field :ip %>
  <%= f.hidden_field :likeable_type %>
  <%= f.hidden_field :likeable_id %>
  <button type="submit" class="btn btn-success">
    <i class="fa fa-thumbs-up fa-fw" aria-hidden="true"></i> Like
  </button>
  <% end %>
</div>
